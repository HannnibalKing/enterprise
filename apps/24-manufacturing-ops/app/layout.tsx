export const metadata = {
  title: "Manufacturing Operations - Enterprise",
  description: "Production planning and monitoring system",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
