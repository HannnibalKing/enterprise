{
  "name": "@enterprise/tls-auditor",
  "version": "1.0.0",
  "description": "Comprehensive TLS/SSL configuration auditor — checks protocol versions, cipher suites, certificate chain, HSTS, and OCSP stapling for any HTTPS endpoint",
  "main": "dist/index.js",
  "bin": {
    "tls-auditor": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/cli.ts",
    "start": "node dist/cli.js",
    "test": "jest --coverage"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
